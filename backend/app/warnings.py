"""warnings 统一文案。

**已于 2026-09-16 全组冻结，逐字不得改写。** 见 docs/00_协作规范.md §5.4。
**2026-09-17 新增 4、5 两类**（周边交通接口 API 10，见执行书 §4.11 / §4.12）；
**同日经全组确认再新增第 6 类**（专业接口 API 11 / 13，
见 docs/02_专业API对接文档.md）。原有五类一字未动。

后端负责判断触发，前端只负责展示，不各自编。
返回字符串数组，不做错误码体系，前端统一去重展示。

需要新增文案时先提，不要临时编。
"""

# 触发：空间筛选确实排除了无 Campus 高校
NO_CAMPUS_EXCLUDED = "部分符合招生条件的高校因缺少校区空间数据未参与空间筛选"

# 触发：查询或展示涉及 category 或 batch
SOURCE_VOCABULARY = "当前科类/批次使用来源数据原始口径"

# 触发：空间查询包含 CANDIDATE
INCLUDE_CANDIDATE_CAMPUS = "本次空间查询包含候选校区"

# 触发：指定高校有校区，但半径内一个 poi_transport 点都没有。
# poi_transport 覆盖不齐，抽样 200 个校区只有 73% 能在 3 km 内找到交通点，
# 所以"查不到"是常态，不能让前端显示成"周边没有站点"。
NO_TRANSPORT_DATA = "部分高校周边缺少交通设施数据"

# 触发：指定高校压根没有带几何的校区，周边查询无从做起。
# 全库 2,952 所高校只有 479 所有校区，这是**常见情况**不是边缘情况，
# 所以要和上一条分开说——否则会把"缺校区"错说成"缺交通数据"。
NO_CAMPUS_FOR_QUERY = "该校暂无可用校区数据，无法进行周边查询"

# 触发：查询按**标准专业**筛选（专业接口的 std_major_id 参数，或反查接口
# /api/majors/{major_id}/colleges，后者无条件触发）。
#
# **第 6 类，2026-09-17 随专业接口一并经全组确认，文案已冻结。**
#
# 为什么必须新增而不是复用现有的：专业是两层语义。来源招生专业表达覆盖
# 98.62%，但标准专业（Tier 1 精确映射）只覆盖 37.7984% 的录取事实。
# 按标准专业一筛，**六成以上的记录根本进不了检索范围**。没有这条警告，
# 前端会把"只查到 5 条"直接渲染成"该校只招这 5 个专业"，是实打实的错误结论。
# 现有 5 类里没有一类说的是这件事：SOURCE_VOCABULARY 说的是科类/批次口径，
# 与专业映射无关。
STD_MAJOR_FILTER_COVERAGE = (
    "按标准专业筛选仅覆盖已建立精确映射的录取记录，未建立映射不代表该校未招该专业"
)


def dedupe(items: list[str]) -> list[str]:
    """去重并保持顺序。"""
    seen: set[str] = set()
    out: list[str] = []
    for w in items:
        if w and w not in seen:
            seen.add(w)
            out.append(w)
    return out
